package Semant;

import Types.*;

public class TypeEntry extends Entry {
    // public TypeEntry(Type typ, int level) {
	// 	super(typ, level);
	// }

    public TypeEntry(Type typ) {
		super(typ);
	}
}